# Supabase Functions

Reserve this layer for narrow edge use cases such as signed upload workflows or low-latency partner callbacks.
Core marketplace ranking, filtering, affiliate, and lead-routing logic should remain in the primary backend layer.

