export async function getContentBlock(slug: string) {
  return {
    slug,
    status: "placeholder",
    message: "Editorial content contracts will be sourced from backend-owned content modules.",
  };
}

